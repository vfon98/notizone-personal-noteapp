import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },

  cardList: {
    flexDirection: "row",
  },
  cardItem: {
    marginBottom: 8,
    marginEnd: 8,
    flexBasis: "50%",
  },
  cardButtonAdd: {
    flexBasis: "50%",
    // width: 100,
    height: 100,
    marginBottom: 8,
    marginEnd: 8,
  },
});
